#include "../include/collisions.h"
#include <SDL2/SDL.h>

void testObjectCollision(PlayerType *Playertype, ObjectType *Objecttypes, int numObjects) {
	for (int i = 0; i < numObjects; i++) {
		if (

	}


}
